'use strict';

describe('Reciper.recipes module', function() {

  beforeEach(module('Reciper.recipes'));

  describe('recipes controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var recipesController = $controller('RecipesController');
      expect(recipesController).toBeDefined();
    }));

  });
});